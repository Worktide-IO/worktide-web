# Where the data + auth providers hit the Worktide API.
# Dev default `/v1` is proxied to https://api.worktide.ddev.site by Vite.
# Production deploys point this at the real backend, e.g.:
#   VITE_API_BASE=https://api.worktide.example.com/v1
VITE_API_BASE=/v1
