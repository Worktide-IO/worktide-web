/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { documentJsonldSchema } from "./document/jsonldSchema.ts";
import { documentSchema } from "./documentSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

/**
 * @description Document resource created
 */
export const apiDocumentsPost201Schema = z.lazy(() => documentJsonldSchema).describe("Wiki-style document — rich-text body, hierarchical via parent self-FK,\ngrouped into a DocumentSpace OR attached to a Project / Task.\n\nSoft-delete + workspace scope + versioning all standard. Content lives\nin `body` as text; `bodyFormat` tells clients how to render it. For the\nMVP we ship markdown; richtext (JSON tree) is reserved for when we\npick a structured editor.\n\nPrivacy & sharing:\n  isPrivate=true  → only the author + explicit DocumentContributors see it\n  isPrivate=false → workspace members see it (additionally restricted by\n                    isHiddenForConnectUsers for external Project members)\n  Contributors via DocumentContributor entity grant read/manage to specific\n  users beyond the default rules.")

/**
 * @description Invalid input
 */
export const apiDocumentsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiDocumentsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiDocumentsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new Document resource
 */
export const apiDocumentsPostMutationRequestSchema = z.lazy(() => documentSchema).describe("Wiki-style document — rich-text body, hierarchical via parent self-FK,\ngrouped into a DocumentSpace OR attached to a Project / Task.\n\nSoft-delete + workspace scope + versioning all standard. Content lives\nin `body` as text; `bodyFormat` tells clients how to render it. For the\nMVP we ship markdown; richtext (JSON tree) is reserved for when we\npick a structured editor.\n\nPrivacy & sharing:\n  isPrivate=true  → only the author + explicit DocumentContributors see it\n  isPrivate=false → workspace members see it (additionally restricted by\n                    isHiddenForConnectUsers for external Project members)\n  Contributors via DocumentContributor entity grant read/manage to specific\n  users beyond the default rules.").unwrap().omit({ 'contributors': true,'revisions': true,'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'private': true,'hiddenForConnectUsers': true,'archived': true,'deleted': true })

export const apiDocumentsPostMutationResponseSchema = z.lazy(() => apiDocumentsPost201Schema)