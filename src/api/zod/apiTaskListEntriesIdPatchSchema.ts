/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskListEntryJsonMergePatchSchema } from "./taskListEntry/jsonMergePatchSchema.ts";
import { taskListEntryJsonldSchema } from "./taskListEntry/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskListEntriesIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("TaskListEntry identifier")
    })

/**
 * @description TaskListEntry resource updated
 */
export const apiTaskListEntriesIdPatch200Schema = z.lazy(() => taskListEntryJsonldSchema).describe("Membership of a Task in a TaskList, with per-list ordering.\n\nModelled as an explicit join entity (rather than a plain ManyToMany\njunction) so each placement carries its own ordering — moving a task\nbetween lists doesn't lose its position in the others.\n\nUnique constraint on (list, task) prevents the same task being added\nto the same list twice.")

/**
 * @description Invalid input
 */
export const apiTaskListEntriesIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskListEntriesIdPatch403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiTaskListEntriesIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskListEntriesIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated TaskListEntry resource
 */
export const apiTaskListEntriesIdPatchMutationRequestSchema = z.lazy(() => taskListEntryJsonMergePatchSchema).describe("Membership of a Task in a TaskList, with per-list ordering.\n\nModelled as an explicit join entity (rather than a plain ManyToMany\njunction) so each placement carries its own ordering — moving a task\nbetween lists doesn't lose its position in the others.\n\nUnique constraint on (list, task) prevents the same task being added\nto the same list twice.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true })

export const apiTaskListEntriesIdPatchMutationResponseSchema = z.lazy(() => apiTaskListEntriesIdPatch200Schema)