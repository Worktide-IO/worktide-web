/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { workflowTransitionJsonMergePatchSchema } from "./workflowTransition/jsonMergePatchSchema.ts";
import { workflowTransitionJsonldSchema } from "./workflowTransition/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiWorkflowTransitionsIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("WorkflowTransition identifier")
    })

/**
 * @description WorkflowTransition resource updated
 */
export const apiWorkflowTransitionsIdPatch200Schema = z.lazy(() => workflowTransitionJsonldSchema).describe("One legal status transition in the workflow engine.\n\nThe shape is (Tracker × fromStatus → toStatus) with an optional set\nof WorkspaceMemberRole strings that may perform it. The full state\nmachine for a Tracker is the union of all its WorkflowTransition\nrows.\n\n## Default-open semantics\n\nThe intent of this entity is to *constrain* a workflow, not to define\none from scratch. So:\n\n - If a workspace has **zero** WorkflowTransition rows for a given\n   (tracker, fromStatus), all moves out of fromStatus are allowed\n   for every role. This keeps existing workspaces working without\n   forced seeding.\n - As soon as **one** row exists for (tracker, fromStatus), the\n   workspace has opted into a closed state machine: only the\n   declared toStatus values are accepted, and only for the listed\n   roles. Anything else is rejected by the policy service.\n\nWorkspace owners and admins always bypass the check — they need to\nfix broken workflows without having to grant themselves a role on\nevery transition.\n\nThe `tracker` and `allowedRoles` columns are nullable so a workspace\ncan author a coarse \"any tracker, any role\" baseline before\nnarrowing later — null tracker means \"applies to every tracker that\n * doesn't have its own explicit rules\"; an empty allowedRoles array\nmeans \"no role can perform this\" (a useful kill-switch).")

/**
 * @description Invalid input
 */
export const apiWorkflowTransitionsIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiWorkflowTransitionsIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiWorkflowTransitionsIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated WorkflowTransition resource
 */
export const apiWorkflowTransitionsIdPatchMutationRequestSchema = z.lazy(() => workflowTransitionJsonMergePatchSchema).describe("One legal status transition in the workflow engine.\n\nThe shape is (Tracker × fromStatus → toStatus) with an optional set\nof WorkspaceMemberRole strings that may perform it. The full state\nmachine for a Tracker is the union of all its WorkflowTransition\nrows.\n\n## Default-open semantics\n\nThe intent of this entity is to *constrain* a workflow, not to define\none from scratch. So:\n\n - If a workspace has **zero** WorkflowTransition rows for a given\n   (tracker, fromStatus), all moves out of fromStatus are allowed\n   for every role. This keeps existing workspaces working without\n   forced seeding.\n - As soon as **one** row exists for (tracker, fromStatus), the\n   workspace has opted into a closed state machine: only the\n   declared toStatus values are accepted, and only for the listed\n   roles. Anything else is rejected by the policy service.\n\nWorkspace owners and admins always bypass the check — they need to\nfix broken workflows without having to grant themselves a role on\nevery transition.\n\nThe `tracker` and `allowedRoles` columns are nullable so a workspace\ncan author a coarse \"any tracker, any role\" baseline before\nnarrowing later — null tracker means \"applies to every tracker that\n * doesn't have its own explicit rules\"; an empty allowedRoles array\nmeans \"no role can perform this\" (a useful kill-switch).").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true })

export const apiWorkflowTransitionsIdPatchMutationResponseSchema = z.lazy(() => apiWorkflowTransitionsIdPatch200Schema)