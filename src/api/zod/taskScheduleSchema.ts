/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

/**
 * @description \"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained.
 */
export const taskScheduleSchema = z.object({
    "project": z.optional(z.string()),
"name": z.optional(z.string()),
"cronExpression": z.optional(z.string().default("0 9 * * 1").describe("Standard 5-field cron expression: `m h dom mon dow`")),
"timezone": z.optional(z.string().default("Europe/Berlin")),
"nextRunAt": z.iso.datetime().nullish(),
"lastRunAt": z.iso.datetime().nullish(),
"isEnabled": z.optional(z.boolean().default(true)),
"taskTitle": z.optional(z.string()),
"taskDescription": z.string().nullish(),
"taskPriority": z.optional(z.string().default("normal")),
"taskEstimatedMinutes": z.int().nullish(),
"taskAssignee": z.string().nullish(),
"id": z.uuid().nullish(),
"createdAt": z.optional(z.iso.datetime()),
"updatedAt": z.optional(z.iso.datetime()),
"workspace": z.optional(z.string()),
"version": z.optional(z.int().default(1)),
"createdByUser": z.string().nullish(),
"updatedByUser": z.string().nullish(),
"enabled": z.optional(z.boolean())
    }).describe("\"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained.")