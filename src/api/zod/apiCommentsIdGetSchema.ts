/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { commentJsonldSchema } from "./comment/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiCommentsIdGetPathParamsSchema = z.object({
    "id": z.string().describe("Comment identifier")
    })

/**
 * @description Comment resource
 */
export const apiCommentsIdGet200Schema = z.lazy(() => commentJsonldSchema).describe("Polymorphic comment attached to a Project / Task / Document (extensible).\n\nMirrors awork's pinning + threading semantics:\n - `parent` enables one-level reply threads\n - `pinnedAt` + `pinnedBy` flag a comment as \"important\" (sticks to the top)\n - `editedAt` differentiates the original post from edits (clients badge it)\n - `mentions` is a JSON array of user UUIDs referenced via @-syntax; the\n   notification subsystem (later) will read this column.\n\nPrivacy: comments inherit visibility from their parent — voter delegates.")

/**
 * @description Forbidden
 */
export const apiCommentsIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiCommentsIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiCommentsIdGetQueryResponseSchema = z.lazy(() => apiCommentsIdGet200Schema)