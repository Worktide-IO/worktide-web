/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { autopilotJsonldSchema } from "./autopilot/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { z } from "zod/v4";

export const apiAutopilotsGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string())),
"project": z.optional(z.string()),
"project[]": z.optional(z.array(z.string())),
"isEnabled": z.optional(z.boolean())
    })

/**
 * @description Autopilot collection
 */
export const apiAutopilotsGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(autopilotJsonldSchema.describe("Per-Project alert rules — \"warn the project lead when budget > 80%\n * consumed\", \"ping when a task is overdue by more than 3 days\".\n\nOne Autopilot per project (UNIQUE on project_id). Rules are stored as\nJSON so adding new alert kinds doesn't require schema migrations; the\nAutopilotEvaluator service knows how to interpret each kind.\n\nAvailable alert kinds (B8 MVP):\n  - \"budget_threshold\"     { percent: 80 }\n  - \"overdue_tasks\"        { gracePeriodDays: 0 }\n  - \"due_soon\"             { withinDays: 3 }\n\nEach rule that triggers writes a DomainEvent named\n\"autopilot.{kind}.triggered\" carrying the projectId + payload, plus\nstamps lastTriggeredAt on the Autopilot. Consumers (notifications,\nwebhooks once B10 lands) subscribe to these events."))
              }
    })).describe("Autopilot.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiAutopilotsGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiAutopilotsGetCollectionQueryResponseSchema = z.lazy(() => apiAutopilotsGetCollection200Schema)