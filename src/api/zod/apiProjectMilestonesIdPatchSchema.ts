/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { projectMilestoneJsonMergePatchSchema } from "./projectMilestone/jsonMergePatchSchema.ts";
import { projectMilestoneJsonldSchema } from "./projectMilestone/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiProjectMilestonesIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("ProjectMilestone identifier")
    })

/**
 * @description ProjectMilestone resource updated
 */
export const apiProjectMilestonesIdPatch200Schema = z.lazy(() => projectMilestoneJsonldSchema).describe("A named goal/checkpoint inside a Project — typically tied to a due date,\nwith an optional set of tasks whose completion marks it as reached.\n\nGoing beyond awork (which only has id/name/color/dueDate):\n - `isReached` + `reachedOn` + `reachedBy` so the activity feed knows when\n - `isArchived` to hide stale milestones without losing history\n - M:N to Task — \"what needs to be done for this milestone\"\n - `description` + `position` for richer UI")

/**
 * @description Invalid input
 */
export const apiProjectMilestonesIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiProjectMilestonesIdPatch403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiProjectMilestonesIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiProjectMilestonesIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated ProjectMilestone resource
 */
export const apiProjectMilestonesIdPatchMutationRequestSchema = z.lazy(() => projectMilestoneJsonMergePatchSchema).describe("A named goal/checkpoint inside a Project — typically tied to a due date,\nwith an optional set of tasks whose completion marks it as reached.\n\nGoing beyond awork (which only has id/name/color/dueDate):\n - `isReached` + `reachedOn` + `reachedBy` so the activity feed knows when\n - `isArchived` to hide stale milestones without losing history\n - M:N to Task — \"what needs to be done for this milestone\"\n - `description` + `position` for richer UI").unwrap().omit({ 'reachedOn': true,'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'reached': true,'archived': true,'deleted': true })

export const apiProjectMilestonesIdPatchMutationResponseSchema = z.lazy(() => apiProjectMilestonesIdPatch200Schema)