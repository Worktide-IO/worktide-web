/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { commentJsonldSchema } from "./comment/jsonldSchema.ts";
import { commentSchema } from "./commentSchema.ts";
import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

/**
 * @description Comment resource created
 */
export const apiCommentsPost201Schema = z.lazy(() => commentJsonldSchema).describe("Polymorphic comment attached to a Project / Task / Document (extensible).\n\nMirrors awork's pinning + threading semantics:\n - `parent` enables one-level reply threads\n - `pinnedAt` + `pinnedBy` flag a comment as \"important\" (sticks to the top)\n - `editedAt` differentiates the original post from edits (clients badge it)\n - `mentions` is a JSON array of user UUIDs referenced via @-syntax; the\n   notification subsystem (later) will read this column.\n\nPrivacy: comments inherit visibility from their parent — voter delegates.")

/**
 * @description Invalid input
 */
export const apiCommentsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiCommentsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiCommentsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new Comment resource
 */
export const apiCommentsPostMutationRequestSchema = z.lazy(() => commentSchema).describe("Polymorphic comment attached to a Project / Task / Document (extensible).\n\nMirrors awork's pinning + threading semantics:\n - `parent` enables one-level reply threads\n - `pinnedAt` + `pinnedBy` flag a comment as \"important\" (sticks to the top)\n - `editedAt` differentiates the original post from edits (clients badge it)\n - `mentions` is a JSON array of user UUIDs referenced via @-syntax; the\n   notification subsystem (later) will read this column.\n\nPrivacy: comments inherit visibility from their parent — voter delegates.").unwrap().omit({ 'editedAt': true,'pinnedAt': true,'pinnedBy': true,'reactions': true,'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'pinned': true,'resolved': true,'hiddenForConnectUsers': true,'deleted': true })

export const apiCommentsPostMutationResponseSchema = z.lazy(() => apiCommentsPost201Schema)