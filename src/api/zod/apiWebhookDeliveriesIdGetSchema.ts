/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { webhookDeliveryJsonldSchema } from "./webhookDelivery/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiWebhookDeliveriesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("WebhookDelivery identifier")
    })

/**
 * @description WebhookDelivery resource
 */
export const apiWebhookDeliveriesIdGet200Schema = z.lazy(() => webhookDeliveryJsonldSchema).describe("Single attempt to deliver one DomainEvent to one Webhook. One log row per\n(webhook, event, attempt) — retries get their own row so the timeline is\ncomplete.\n\nRead-only via the API (operators inspect the log; retries happen on the\nMessenger transport's own schedule, not via REST).")

/**
 * @description Forbidden
 */
export const apiWebhookDeliveriesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiWebhookDeliveriesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiWebhookDeliveriesIdGetQueryResponseSchema = z.lazy(() => apiWebhookDeliveriesIdGet200Schema)