/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskListJsonldSchema } from "./taskList/jsonldSchema.ts";
import { taskListSchema } from "./taskListSchema.ts";
import { z } from "zod/v4";

/**
 * @description TaskList resource created
 */
export const apiTaskListsPost201Schema = z.lazy(() => taskListJsonldSchema).describe("Kanban-style \"lane\" / \"column\" inside a Project. Mirrors awork's TaskList:\na Task can sit in MULTIPLE TaskLists (e.g. one Kanban view per quarter +\none priority view) — membership + per-list ordering live in TaskListEntry.\n\nVoter delegates to the parent Project: anyone with VIEW on the Project\nsees the list; EDIT to mutate.")

/**
 * @description Invalid input
 */
export const apiTaskListsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskListsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskListsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new TaskList resource
 */
export const apiTaskListsPostMutationRequestSchema = z.lazy(() => taskListSchema).describe("Kanban-style \"lane\" / \"column\" inside a Project. Mirrors awork's TaskList:\na Task can sit in MULTIPLE TaskLists (e.g. one Kanban view per quarter +\none priority view) — membership + per-list ordering live in TaskListEntry.\n\nVoter delegates to the parent Project: anyone with VIEW on the Project\nsees the list; EDIT to mutate.").unwrap().omit({ 'entries': true,'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'archived': true,'hiddenForConnectUsers': true,'deleted': true })

export const apiTaskListsPostMutationResponseSchema = z.lazy(() => apiTaskListsPost201Schema)