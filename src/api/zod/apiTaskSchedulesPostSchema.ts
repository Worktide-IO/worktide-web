/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskScheduleJsonldSchema } from "./taskSchedule/jsonldSchema.ts";
import { taskScheduleSchema } from "./taskScheduleSchema.ts";
import { z } from "zod/v4";

/**
 * @description TaskSchedule resource created
 */
export const apiTaskSchedulesPost201Schema = z.lazy(() => taskScheduleJsonldSchema).describe("\"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained.")

/**
 * @description Invalid input
 */
export const apiTaskSchedulesPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskSchedulesPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskSchedulesPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new TaskSchedule resource
 */
export const apiTaskSchedulesPostMutationRequestSchema = z.lazy(() => taskScheduleSchema).describe("\"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true,'enabled': true })

export const apiTaskSchedulesPostMutationResponseSchema = z.lazy(() => apiTaskSchedulesPost201Schema)