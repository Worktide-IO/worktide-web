/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { personalAccessTokenJsonldSchema } from "./personalAccessToken/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiPersonalAccessTokensGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string())),
"owner": z.optional(z.string()),
"owner[]": z.optional(z.array(z.string())),
"name": z.optional(z.string())
    })

/**
 * @description PersonalAccessToken collection
 */
export const apiPersonalAccessTokensGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(personalAccessTokenJsonldSchema.describe("Long-lived bearer token a user mints for an external system (CI, custom\ndashboard, integration script). Authenticates against /v1/* the same way\na JWT does, but doesn't expire on the 1-hour cycle.\n\nThe token plaintext is shown EXACTLY ONCE — in the response to the POST\nthat creates it (see PersonalAccessTokenIssueProcessor). After that only\nthe SHA-256 hash sits in the DB, so leaked tokens can be revoked without\nexposing the value to the recovery process.\n\nScope strings let the operator narrow what the token can do:\n  - [\"*\"]            — full ROLE_USER (default)\n  - [\"read:*\"]       — only safe-method endpoints\n  - [\"projects:read\",\"tasks:write\"]  — fine-grained per-resource\n\nScopes are advisory in the firewall — Voters still enforce VIEW/EDIT/…\nauthorization. They primarily limit a stolen token's blast radius."))
              }
    })).describe("PersonalAccessToken.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiPersonalAccessTokensGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiPersonalAccessTokensGetCollectionQueryResponseSchema = z.lazy(() => apiPersonalAccessTokensGetCollection200Schema)