/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { webhookJsonMergePatchSchema } from "./webhook/jsonMergePatchSchema.ts";
import { webhookJsonldSchema } from "./webhook/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiWebhooksIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("Webhook identifier")
    })

/**
 * @description Webhook resource updated
 */
export const apiWebhooksIdPatch200Schema = z.lazy(() => webhookJsonldSchema).describe("Subscription that mirrors selected DomainEvents to an external HTTP endpoint.\n\nEach delivery is signed with HMAC-SHA256 over the request body, using `secret`\nas the key; consumers verify by recomputing the digest and comparing it to the\n`X-Worktide-Signature: sha256=…` header.\n\nEvent subscription matches by name:\n  - `[\"*\"]`                  — every event\n  - `[\"task.*\"]`             — every event whose name starts with \"task.\"\n  - `[\"task.created\"]`       — exact name only\n\nThe `secret` is write-only via the API (returned obfuscated on reads) so it\nnever leaks back to clients after creation. `failureCount` and\n`lastTriggeredAt` are server-managed; clients cannot patch them.\n\nAfter {@see self::FAILURE_THRESHOLD} consecutive non-2xx deliveries the\nwebhook auto-deactivates — the operator must inspect the deliveries log and\nre-enable it with a fresh PATCH.")

/**
 * @description Invalid input
 */
export const apiWebhooksIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiWebhooksIdPatch403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiWebhooksIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiWebhooksIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated Webhook resource
 */
export const apiWebhooksIdPatchMutationRequestSchema = z.lazy(() => webhookJsonMergePatchSchema).describe("Subscription that mirrors selected DomainEvents to an external HTTP endpoint.\n\nEach delivery is signed with HMAC-SHA256 over the request body, using `secret`\nas the key; consumers verify by recomputing the digest and comparing it to the\n`X-Worktide-Signature: sha256=…` header.\n\nEvent subscription matches by name:\n  - `[\"*\"]`                  — every event\n  - `[\"task.*\"]`             — every event whose name starts with \"task.\"\n  - `[\"task.created\"]`       — exact name only\n\nThe `secret` is write-only via the API (returned obfuscated on reads) so it\nnever leaks back to clients after creation. `failureCount` and\n`lastTriggeredAt` are server-managed; clients cannot patch them.\n\nAfter {@see self::FAILURE_THRESHOLD} consecutive non-2xx deliveries the\nwebhook auto-deactivates — the operator must inspect the deliveries log and\nre-enable it with a fresh PATCH.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true,'active': true })

export const apiWebhooksIdPatchMutationResponseSchema = z.lazy(() => apiWebhooksIdPatch200Schema)