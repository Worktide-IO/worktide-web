/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { documentJsonldSchema } from "./document/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiDocumentsIdGetPathParamsSchema = z.object({
    "id": z.string().describe("Document identifier")
    })

/**
 * @description Document resource
 */
export const apiDocumentsIdGet200Schema = z.lazy(() => documentJsonldSchema).describe("Wiki-style document — rich-text body, hierarchical via parent self-FK,\ngrouped into a DocumentSpace OR attached to a Project / Task.\n\nSoft-delete + workspace scope + versioning all standard. Content lives\nin `body` as text; `bodyFormat` tells clients how to render it. For the\nMVP we ship markdown; richtext (JSON tree) is reserved for when we\npick a structured editor.\n\nPrivacy & sharing:\n  isPrivate=true  → only the author + explicit DocumentContributors see it\n  isPrivate=false → workspace members see it (additionally restricted by\n                    isHiddenForConnectUsers for external Project members)\n  Contributors via DocumentContributor entity grant read/manage to specific\n  users beyond the default rules.")

/**
 * @description Forbidden
 */
export const apiDocumentsIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiDocumentsIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiDocumentsIdGetQueryResponseSchema = z.lazy(() => apiDocumentsIdGet200Schema)