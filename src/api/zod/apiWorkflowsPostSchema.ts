/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { workflowJsonldSchema } from "./workflow/jsonldSchema.ts";
import { workflowSchema } from "./workflowSchema.ts";
import { z } from "zod/v4";

/**
 * @description Workflow resource created
 */
export const apiWorkflowsPost201Schema = z.lazy(() => workflowJsonldSchema).describe("Workflow = named process template, linked to N projects. Holds N\nAutomations (when-then rules) that fire on domain events emitted by\nany project bound to this workflow.\n\nLinked from Project.workflow (nullable). awork also allows linking from\nProjectTemplate so instantiated projects inherit it — that comes later.")

/**
 * @description Invalid input
 */
export const apiWorkflowsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiWorkflowsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiWorkflowsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new Workflow resource
 */
export const apiWorkflowsPostMutationRequestSchema = z.lazy(() => workflowSchema).describe("Workflow = named process template, linked to N projects. Holds N\nAutomations (when-then rules) that fire on domain events emitted by\nany project bound to this workflow.\n\nLinked from Project.workflow (nullable). awork also allows linking from\nProjectTemplate so instantiated projects inherit it — that comes later.").unwrap().omit({ 'automations': true,'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'enabled': true,'archived': true,'deleted': true })

export const apiWorkflowsPostMutationResponseSchema = z.lazy(() => apiWorkflowsPost201Schema)