/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { conversationJsonldSchema } from "./conversation/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { z } from "zod/v4";

export const apiConversationsGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string())),
"channel": z.optional(z.string()),
"channel[]": z.optional(z.array(z.string())),
"status": z.optional(z.string()),
"status[]": z.optional(z.array(z.string())),
"customer": z.optional(z.string()),
"customer[]": z.optional(z.array(z.string())),
"assignee": z.optional(z.string()),
"assignee[]": z.optional(z.array(z.string())),
"order[lastEventAt]": z.enum(["asc", "desc"]).default("asc"),
"order[createdAt]": z.enum(["asc", "desc"]).default("asc"),
"order[subject]": z.enum(["asc", "desc"]).default("asc")
    })

/**
 * @description Conversation collection
 */
export const apiConversationsGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(conversationJsonldSchema.describe("Aggregate of {@see InboundEvent}s + {@see OutboundMessage}s that\nbelong to the same logical thread.\n\nOnly threading-capable channels populate this entity — Mail (via\nMessage-ID / References), Slack (thread_ts), WhatsApp (chat-id).\nChannels without thread semantics (Zabbix alerts, CVE feeds,\nmonitoring webhooks) leave their InboundEvents standalone with\n`conversationId = NULL`.\n\n`threadKey` is the channel-specific identifier the\n`ConversationThreader` strategy uses to match incoming events to\nan existing conversation. For mail it's the root References\nvalue; for slack it's the thread_ts of the root message.\n\n`customer`/`assignee` are resolved at first ingest time and may be\noverridden manually in the SPA later."))
              }
    })).describe("Conversation.jsonld collection.")

export const apiConversationsGetCollectionQueryResponseSchema = z.lazy(() => apiConversationsGetCollection200Schema)