/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { autopilotJsonldSchema } from "./autopilot/jsonldSchema.ts";
import { autopilotSchema } from "./autopilotSchema.ts";
import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

/**
 * @description Autopilot resource created
 */
export const apiAutopilotsPost201Schema = z.lazy(() => autopilotJsonldSchema).describe("Per-Project alert rules — \"warn the project lead when budget > 80%\n * consumed\", \"ping when a task is overdue by more than 3 days\".\n\nOne Autopilot per project (UNIQUE on project_id). Rules are stored as\nJSON so adding new alert kinds doesn't require schema migrations; the\nAutopilotEvaluator service knows how to interpret each kind.\n\nAvailable alert kinds (B8 MVP):\n  - \"budget_threshold\"     { percent: 80 }\n  - \"overdue_tasks\"        { gracePeriodDays: 0 }\n  - \"due_soon\"             { withinDays: 3 }\n\nEach rule that triggers writes a DomainEvent named\n\"autopilot.{kind}.triggered\" carrying the projectId + payload, plus\nstamps lastTriggeredAt on the Autopilot. Consumers (notifications,\nwebhooks once B10 lands) subscribe to these events.")

/**
 * @description Invalid input
 */
export const apiAutopilotsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiAutopilotsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiAutopilotsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new Autopilot resource
 */
export const apiAutopilotsPostMutationRequestSchema = z.lazy(() => autopilotSchema).describe("Per-Project alert rules — \"warn the project lead when budget > 80%\n * consumed\", \"ping when a task is overdue by more than 3 days\".\n\nOne Autopilot per project (UNIQUE on project_id). Rules are stored as\nJSON so adding new alert kinds doesn't require schema migrations; the\nAutopilotEvaluator service knows how to interpret each kind.\n\nAvailable alert kinds (B8 MVP):\n  - \"budget_threshold\"     { percent: 80 }\n  - \"overdue_tasks\"        { gracePeriodDays: 0 }\n  - \"due_soon\"             { withinDays: 3 }\n\nEach rule that triggers writes a DomainEvent named\n\"autopilot.{kind}.triggered\" carrying the projectId + payload, plus\nstamps lastTriggeredAt on the Autopilot. Consumers (notifications,\nwebhooks once B10 lands) subscribe to these events.").unwrap().omit({ 'lastTriggeredAt': true,'lastEvaluatedAt': true,'id': true,'createdAt': true,'updatedAt': true,'version': true,'enabled': true })

export const apiAutopilotsPostMutationResponseSchema = z.lazy(() => apiAutopilotsPost201Schema)