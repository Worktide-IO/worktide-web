/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskAssigneeJsonldSchema } from "./taskAssignee/jsonldSchema.ts";
import { taskAssigneeSchema } from "./taskAssigneeSchema.ts";
import { z } from "zod/v4";

/**
 * @description TaskAssignee resource created
 */
export const apiTaskAssigneesPost201Schema = z.lazy(() => taskAssigneeJsonldSchema).describe("Polymorphic Task → (User|Team) assignment.\n\nReplaces the old `task_assignees(task_id, user_id)` ManyToMany join.\nEach row is one assignment of a Principal (a User OR a Team) to a\nTask. The Task's \"effective assignees\" — what shows up in the avatar\nstack and notifications — is the union of:\n  - direct user assignments (principalType=user)\n  - the members of every assigned team (principalType=team, expanded\n    via team_members)\n\nWorkspace is denormalised from the parent task at persist-time so\nfilters and voters don't need a join.\n\nThe principal_type + principal_id pattern intentionally avoids\nDoctrine STI — see AssigneePrincipalType.")

/**
 * @description Invalid input
 */
export const apiTaskAssigneesPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskAssigneesPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskAssigneesPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new TaskAssignee resource
 */
export const apiTaskAssigneesPostMutationRequestSchema = z.lazy(() => taskAssigneeSchema).describe("Polymorphic Task → (User|Team) assignment.\n\nReplaces the old `task_assignees(task_id, user_id)` ManyToMany join.\nEach row is one assignment of a Principal (a User OR a Team) to a\nTask. The Task's \"effective assignees\" — what shows up in the avatar\nstack and notifications — is the union of:\n  - direct user assignments (principalType=user)\n  - the members of every assigned team (principalType=team, expanded\n    via team_members)\n\nWorkspace is denormalised from the parent task at persist-time so\nfilters and voters don't need a join.\n\nThe principal_type + principal_id pattern intentionally avoids\nDoctrine STI — see AssigneePrincipalType.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'principalIri': true })

export const apiTaskAssigneesPostMutationResponseSchema = z.lazy(() => apiTaskAssigneesPost201Schema)