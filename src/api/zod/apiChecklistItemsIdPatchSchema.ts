/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { checklistItemJsonMergePatchSchema } from "./checklistItem/jsonMergePatchSchema.ts";
import { checklistItemJsonldSchema } from "./checklistItem/jsonldSchema.ts";
import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiChecklistItemsIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("ChecklistItem identifier")
    })

/**
 * @description ChecklistItem resource updated
 */
export const apiChecklistItemsIdPatch200Schema = z.lazy(() => checklistItemJsonldSchema).describe("Sub-todo on a Task. Lightweight by design — for proper sub-work items use\nTask.parent (subtasks). Checklists are visual tick-boxes shown inside the\ntask detail panel.\n\nGoing beyond awork: we track who checked the item off and when, so the\nactivity feed can attribute the tick to a user (awork's schema doesn't\nexpose checkedBy / checkedOn).")

/**
 * @description Invalid input
 */
export const apiChecklistItemsIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiChecklistItemsIdPatch403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiChecklistItemsIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiChecklistItemsIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated ChecklistItem resource
 */
export const apiChecklistItemsIdPatchMutationRequestSchema = z.lazy(() => checklistItemJsonMergePatchSchema).describe("Sub-todo on a Task. Lightweight by design — for proper sub-work items use\nTask.parent (subtasks). Checklists are visual tick-boxes shown inside the\ntask detail panel.\n\nGoing beyond awork: we track who checked the item off and when, so the\nactivity feed can attribute the tick to a user (awork's schema doesn't\nexpose checkedBy / checkedOn).").unwrap().omit({ 'checkedOn': true,'id': true,'createdAt': true,'updatedAt': true,'version': true,'done': true })

export const apiChecklistItemsIdPatchMutationResponseSchema = z.lazy(() => apiChecklistItemsIdPatch200Schema)