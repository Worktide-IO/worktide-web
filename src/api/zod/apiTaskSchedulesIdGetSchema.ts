/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskScheduleJsonldSchema } from "./taskSchedule/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskSchedulesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("TaskSchedule identifier")
    })

/**
 * @description TaskSchedule resource
 */
export const apiTaskSchedulesIdGet200Schema = z.lazy(() => taskScheduleJsonldSchema).describe("\"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained.")

/**
 * @description Forbidden
 */
export const apiTaskSchedulesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiTaskSchedulesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiTaskSchedulesIdGetQueryResponseSchema = z.lazy(() => apiTaskSchedulesIdGet200Schema)