/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { documentRevisionJsonldSchema } from "./documentRevision/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { z } from "zod/v4";

export const apiDocumentRevisionsGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"document": z.optional(z.string()),
"document[]": z.optional(z.array(z.string())),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string()))
    })

/**
 * @description DocumentRevision collection
 */
export const apiDocumentRevisionsGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(documentRevisionJsonldSchema.describe("Append-only snapshot of a Document at one save-point. Lets the wiki\nUI walk back through history and restore older bodies.\n\nCreated by DocumentRevisionListener whenever a Document's name or\nbody changes (preUpdate). The pre-existing values are snapshotted —\nthe listener writes the LAST KNOWN state, not the new one, so the\nrevision list reads like a real history (\"Stand vom Datum X\").\n\nRevisions are read-only — no Patch/Post via the API. Restoring goes\nthrough a custom action on Document, not by mutating the revision."))
              }
    })).describe("DocumentRevision.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiDocumentRevisionsGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiDocumentRevisionsGetCollectionQueryResponseSchema = z.lazy(() => apiDocumentRevisionsGetCollection200Schema)