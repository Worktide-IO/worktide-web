/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { userCapacityJsonldSchema } from "./userCapacity/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiUserCapacitiesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("UserCapacity identifier")
    })

/**
 * @description UserCapacity resource
 */
export const apiUserCapacitiesIdGet200Schema = z.lazy(() => userCapacityJsonldSchema).describe("Per-user weekly working capacity in MINUTES per weekday. Replaces awork's\n`UserWeeklyCapacity` object — flattened into the row so reporting queries\ndon't need to join an embedded.\n\nOne row per user, enforced by the unique constraint on user_id. Default\n= 0 minutes everywhere; UI should pre-fill 480 (8h) on weekdays for new\nusers.")

/**
 * @description Forbidden
 */
export const apiUserCapacitiesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiUserCapacitiesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiUserCapacitiesIdGetQueryResponseSchema = z.lazy(() => apiUserCapacitiesIdGet200Schema)