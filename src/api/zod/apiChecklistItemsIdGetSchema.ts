/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { checklistItemJsonldSchema } from "./checklistItem/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiChecklistItemsIdGetPathParamsSchema = z.object({
    "id": z.string().describe("ChecklistItem identifier")
    })

/**
 * @description ChecklistItem resource
 */
export const apiChecklistItemsIdGet200Schema = z.lazy(() => checklistItemJsonldSchema).describe("Sub-todo on a Task. Lightweight by design — for proper sub-work items use\nTask.parent (subtasks). Checklists are visual tick-boxes shown inside the\ntask detail panel.\n\nGoing beyond awork: we track who checked the item off and when, so the\nactivity feed can attribute the tick to a user (awork's schema doesn't\nexpose checkedBy / checkedOn).")

/**
 * @description Forbidden
 */
export const apiChecklistItemsIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiChecklistItemsIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiChecklistItemsIdGetQueryResponseSchema = z.lazy(() => apiChecklistItemsIdGet200Schema)