/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

/**
 * @description Long-lived bearer token a user mints for an external system (CI, custom\ndashboard, integration script). Authenticates against /v1/* the same way\na JWT does, but doesn\'t expire on the 1-hour cycle.\n\nThe token plaintext is shown EXACTLY ONCE — in the response to the POST\nthat creates it (see PersonalAccessTokenIssueProcessor). After that only\nthe SHA-256 hash sits in the DB, so leaked tokens can be revoked without\nexposing the value to the recovery process.\n\nScope strings let the operator narrow what the token can do:\n  - [\"*\"]            — full ROLE_USER (default)\n  - [\"read:*\"]       — only safe-method endpoints\n  - [\"projects:read\",\"tasks:write\"]  — fine-grained per-resource\n\nScopes are advisory in the firewall — Voters still enforce VIEW/EDIT/…\nauthorization. They primarily limit a stolen token\'s blast radius.
 */
export const personalAccessTokenHtmlSchema = z.object({
    "name": z.optional(z.string().default("")),
"owner": z.optional(z.string()),
"tokenPrefix": z.optional(z.string().default("").describe("Short prefix of the plaintext (the first 8 chars). Lets the operator\nrecognise tokens in lists without exposing the secret. Persisted on\ncreate only.")),
"scopes": z.optional(z.array(z.string())),
"lastUsedAt": z.iso.datetime().nullish(),
"expiresAt": z.iso.datetime().nullish(),
"revokedAt": z.iso.datetime().nullish(),
"plaintextToken": z.string().describe("Plaintext token — only populated by the issue processor on POST so it\nreaches the response body. Not persisted to the DB and not echoed by\nsubsequent GETs (the issue processor is only wired up for POST).").nullish(),
"id": z.uuid().nullish(),
"createdAt": z.optional(z.iso.datetime()),
"updatedAt": z.optional(z.iso.datetime()),
"workspace": z.optional(z.string()),
"createdByUser": z.string().nullish(),
"updatedByUser": z.string().nullish(),
"revoked": z.optional(z.boolean())
    }).describe("Long-lived bearer token a user mints for an external system (CI, custom\ndashboard, integration script). Authenticates against /v1/* the same way\na JWT does, but doesn't expire on the 1-hour cycle.\n\nThe token plaintext is shown EXACTLY ONCE — in the response to the POST\nthat creates it (see PersonalAccessTokenIssueProcessor). After that only\nthe SHA-256 hash sits in the DB, so leaked tokens can be revoked without\nexposing the value to the recovery process.\n\nScope strings let the operator narrow what the token can do:\n  - [\"*\"]            — full ROLE_USER (default)\n  - [\"read:*\"]       — only safe-method endpoints\n  - [\"projects:read\",\"tasks:write\"]  — fine-grained per-resource\n\nScopes are advisory in the firewall — Voters still enforce VIEW/EDIT/…\nauthorization. They primarily limit a stolen token's blast radius.")