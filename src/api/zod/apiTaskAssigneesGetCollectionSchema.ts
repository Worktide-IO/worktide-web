/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { taskAssigneeJsonldSchema } from "./taskAssignee/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskAssigneesGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string())),
"task": z.optional(z.string()),
"task[]": z.optional(z.array(z.string())),
"principalType": z.optional(z.string()),
"principalType[]": z.optional(z.array(z.string())),
"principalId": z.optional(z.string()),
"principalId[]": z.optional(z.array(z.string()))
    })

/**
 * @description TaskAssignee collection
 */
export const apiTaskAssigneesGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(taskAssigneeJsonldSchema.describe("Polymorphic Task → (User|Team) assignment.\n\nReplaces the old `task_assignees(task_id, user_id)` ManyToMany join.\nEach row is one assignment of a Principal (a User OR a Team) to a\nTask. The Task's \"effective assignees\" — what shows up in the avatar\nstack and notifications — is the union of:\n  - direct user assignments (principalType=user)\n  - the members of every assigned team (principalType=team, expanded\n    via team_members)\n\nWorkspace is denormalised from the parent task at persist-time so\nfilters and voters don't need a join.\n\nThe principal_type + principal_id pattern intentionally avoids\nDoctrine STI — see AssigneePrincipalType."))
              }
    })).describe("TaskAssignee.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiTaskAssigneesGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiTaskAssigneesGetCollectionQueryResponseSchema = z.lazy(() => apiTaskAssigneesGetCollection200Schema)