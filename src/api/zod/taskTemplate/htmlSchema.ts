/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { z } from "zod/v4";

/**
 * @description Template for a Task — lives inside a TaskBundle. When the bundle is\napplied to a project (or pulled in via instantiate-from-ProjectTemplate),\neach TaskTemplate becomes a real Task with these defaults filled in and\neverything else (assignee, dueDate, status) reset to project defaults.\n\n`dueDayOffset` is days from the project start; the instantiator computes\n`dueOn = project.startsOn + dueDayOffset` (or `null` if startsOn is null).\n`parent` enables one-level subtask templates inside the same bundle.
 */
export const taskTemplateHtmlSchema = z.object({
    "bundle": z.optional(z.string()),
"title": z.optional(z.string()),
"description": z.string().nullish(),
"priority": z.optional(z.enum(["low", "normal", "high", "urgent"]).default("normal")),
"estimatedMinutes": z.int().nullish(),
"dueDayOffset": z.int().describe("Days from project start when this task is due. Null = no auto due-date.").nullish(),
"position": z.optional(z.int().default(0)),
"parent": z.string().nullish(),
"defaultChecklist": z.optional(z.array(z.string()).describe("default checklist item names — instantiator creates ChecklistItem rows")),
"id": z.uuid().nullish(),
"createdAt": z.optional(z.iso.datetime()),
"updatedAt": z.optional(z.iso.datetime()),
"workspace": z.optional(z.string()),
"version": z.optional(z.int().default(1)),
"createdByUser": z.string().nullish(),
"updatedByUser": z.string().nullish()
    }).describe("Template for a Task — lives inside a TaskBundle. When the bundle is\napplied to a project (or pulled in via instantiate-from-ProjectTemplate),\neach TaskTemplate becomes a real Task with these defaults filled in and\neverything else (assignee, dueDate, status) reset to project defaults.\n\n`dueDayOffset` is days from the project start; the instantiator computes\n`dueOn = project.startsOn + dueDayOffset` (or `null` if startsOn is null).\n`parent` enables one-level subtask templates inside the same bundle.")