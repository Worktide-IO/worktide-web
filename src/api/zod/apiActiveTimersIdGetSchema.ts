/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { activeTimerJsonldSchema } from "./activeTimer/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiActiveTimersIdGetPathParamsSchema = z.object({
    "id": z.string().describe("ActiveTimer identifier")
    })

/**
 * @description ActiveTimer resource
 */
export const apiActiveTimersIdGet200Schema = z.lazy(() => activeTimerJsonldSchema).describe("Currently-running stopwatch for one user. At most one row per user (UNIQUE\non user_id) — starting a new timer auto-stops the previous one and turns it\ninto a finalized TimeEntry.\n\nThis is a deliberately separate entity from TimeEntry so:\n  - TimeEntries stay immutable historical records,\n  - the running stopwatch doesn't pollute reports with a zero-or-partial\n    duration row,\n  - the polling endpoint to render \"Sven is tracking X\" only hits one row.\n\nRead-only via the API; clients drive transitions through the dedicated\n/v1/timers/start and /v1/timers/stop endpoints.")

/**
 * @description Forbidden
 */
export const apiActiveTimersIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiActiveTimersIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiActiveTimersIdGetQueryResponseSchema = z.lazy(() => apiActiveTimersIdGet200Schema)