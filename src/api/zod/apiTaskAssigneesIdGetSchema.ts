/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskAssigneeJsonldSchema } from "./taskAssignee/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskAssigneesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("TaskAssignee identifier")
    })

/**
 * @description TaskAssignee resource
 */
export const apiTaskAssigneesIdGet200Schema = z.lazy(() => taskAssigneeJsonldSchema).describe("Polymorphic Task → (User|Team) assignment.\n\nReplaces the old `task_assignees(task_id, user_id)` ManyToMany join.\nEach row is one assignment of a Principal (a User OR a Team) to a\nTask. The Task's \"effective assignees\" — what shows up in the avatar\nstack and notifications — is the union of:\n  - direct user assignments (principalType=user)\n  - the members of every assigned team (principalType=team, expanded\n    via team_members)\n\nWorkspace is denormalised from the parent task at persist-time so\nfilters and voters don't need a join.\n\nThe principal_type + principal_id pattern intentionally avoids\nDoctrine STI — see AssigneePrincipalType.")

/**
 * @description Forbidden
 */
export const apiTaskAssigneesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiTaskAssigneesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiTaskAssigneesIdGetQueryResponseSchema = z.lazy(() => apiTaskAssigneesIdGet200Schema)