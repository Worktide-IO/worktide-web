/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskListEntryJsonldSchema } from "./taskListEntry/jsonldSchema.ts";
import { taskListEntrySchema } from "./taskListEntrySchema.ts";
import { z } from "zod/v4";

/**
 * @description TaskListEntry resource created
 */
export const apiTaskListEntriesPost201Schema = z.lazy(() => taskListEntryJsonldSchema).describe("Membership of a Task in a TaskList, with per-list ordering.\n\nModelled as an explicit join entity (rather than a plain ManyToMany\njunction) so each placement carries its own ordering — moving a task\nbetween lists doesn't lose its position in the others.\n\nUnique constraint on (list, task) prevents the same task being added\nto the same list twice.")

/**
 * @description Invalid input
 */
export const apiTaskListEntriesPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskListEntriesPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskListEntriesPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new TaskListEntry resource
 */
export const apiTaskListEntriesPostMutationRequestSchema = z.lazy(() => taskListEntrySchema).describe("Membership of a Task in a TaskList, with per-list ordering.\n\nModelled as an explicit join entity (rather than a plain ManyToMany\njunction) so each placement carries its own ordering — moving a task\nbetween lists doesn't lose its position in the others.\n\nUnique constraint on (list, task) prevents the same task being added\nto the same list twice.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true })

export const apiTaskListEntriesPostMutationResponseSchema = z.lazy(() => apiTaskListEntriesPost201Schema)