/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { checklistItemJsonldSchema } from "./checklistItem/jsonldSchema.ts";
import { checklistItemSchema } from "./checklistItemSchema.ts";
import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

/**
 * @description ChecklistItem resource created
 */
export const apiChecklistItemsPost201Schema = z.lazy(() => checklistItemJsonldSchema).describe("Sub-todo on a Task. Lightweight by design — for proper sub-work items use\nTask.parent (subtasks). Checklists are visual tick-boxes shown inside the\ntask detail panel.\n\nGoing beyond awork: we track who checked the item off and when, so the\nactivity feed can attribute the tick to a user (awork's schema doesn't\nexpose checkedBy / checkedOn).")

/**
 * @description Invalid input
 */
export const apiChecklistItemsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiChecklistItemsPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiChecklistItemsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new ChecklistItem resource
 */
export const apiChecklistItemsPostMutationRequestSchema = z.lazy(() => checklistItemSchema).describe("Sub-todo on a Task. Lightweight by design — for proper sub-work items use\nTask.parent (subtasks). Checklists are visual tick-boxes shown inside the\ntask detail panel.\n\nGoing beyond awork: we track who checked the item off and when, so the\nactivity feed can attribute the tick to a user (awork's schema doesn't\nexpose checkedBy / checkedOn).").unwrap().omit({ 'checkedOn': true,'id': true,'createdAt': true,'updatedAt': true,'version': true,'done': true })

export const apiChecklistItemsPostMutationResponseSchema = z.lazy(() => apiChecklistItemsPost201Schema)