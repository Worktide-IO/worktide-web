/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { channelJsonldSchema } from "./channel/jsonldSchema.ts";
import { channelSchema } from "./channelSchema.ts";
import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

/**
 * @description Channel resource created
 */
export const apiChannelsPost201Schema = z.lazy(() => channelJsonldSchema).describe("Source-agnostic communication endpoint for a workspace.\n\nA Channel is a configuration row that binds an in-code adapter\n(`adapterCode` — e.g. `email_imap`, `email_graph`, `slack_bot`,\n`zabbix_webhook`) to a workspace, holds its auth + endpoint config,\nand declares whether it carries inbound, outbound, or both.\n\nMails are the canonical both-capable channel (IMAP-Pull + SMTP-Send\non the same Mailbox). A Zabbix webhook is inbound-only. A\ntransactional Mailgun setup is outbound-only.\n\nTier-1 implementation only ships the three email adapters\n(email_imap / email_graph / email_gmail). Slack/Zabbix/Twilio/etc.\ndock onto this entity in later phases without a schema migration —\nadding a new value to the `adapterCode` whitelist is enough.\n\n`authConfig` is stored as JSON and is meant to be **libsodium-\nencrypted at rest** by an Doctrine event-listener (TBD in C.2). The\nraw column never holds passwords/tokens in cleartext.\n\n`inboundConfig` / `outboundConfig` are adapter-specific shapes\n(IMAP host/port/folder, Graph user-id, Gmail labelIds, …). The\nadapter declares its expected schema; this entity stores it\nopaque.")

/**
 * @description Invalid input
 */
export const apiChannelsPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiChannelsPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new Channel resource
 */
export const apiChannelsPostMutationRequestSchema = z.lazy(() => channelSchema).describe("Source-agnostic communication endpoint for a workspace.\n\nA Channel is a configuration row that binds an in-code adapter\n(`adapterCode` — e.g. `email_imap`, `email_graph`, `slack_bot`,\n`zabbix_webhook`) to a workspace, holds its auth + endpoint config,\nand declares whether it carries inbound, outbound, or both.\n\nMails are the canonical both-capable channel (IMAP-Pull + SMTP-Send\non the same Mailbox). A Zabbix webhook is inbound-only. A\ntransactional Mailgun setup is outbound-only.\n\nTier-1 implementation only ships the three email adapters\n(email_imap / email_graph / email_gmail). Slack/Zabbix/Twilio/etc.\ndock onto this entity in later phases without a schema migration —\nadding a new value to the `adapterCode` whitelist is enough.\n\n`authConfig` is stored as JSON and is meant to be **libsodium-\nencrypted at rest** by an Doctrine event-listener (TBD in C.2). The\nraw column never holds passwords/tokens in cleartext.\n\n`inboundConfig` / `outboundConfig` are adapter-specific shapes\n(IMAP host/port/folder, Graph user-id, Gmail labelIds, …). The\nadapter declares its expected schema; this entity stores it\nopaque.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'entitySyncEnabled': true,'shared': true,'enabled': true,'deleted': true })

export const apiChannelsPostMutationResponseSchema = z.lazy(() => apiChannelsPost201Schema)