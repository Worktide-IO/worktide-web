/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { absenceJsonldSchema } from "./absence/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiAbsencesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("Absence identifier")
    })

/**
 * @description Absence resource
 */
export const apiAbsencesIdGet200Schema = z.lazy(() => absenceJsonldSchema).describe("Personal absence — vacation, sick leave, etc. Linked to a user; counts\nagainst their UserCapacity when computing availability.\n\nBeyond awork: we have a `type` enum (vacation / sick / personal /\nholiday / other) for filtering and reporting. awork doesn't model this,\nso its UI relies on `description` heuristics.\n\nHalf-day flags follow awork's semantics: if both start and end are the\nsame date, only one half-day flag matters.")

/**
 * @description Forbidden
 */
export const apiAbsencesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiAbsencesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiAbsencesIdGetQueryResponseSchema = z.lazy(() => apiAbsencesIdGet200Schema)