/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { conversationJsonMergePatchSchema } from "./conversation/jsonMergePatchSchema.ts";
import { conversationJsonldSchema } from "./conversation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiConversationsIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("Conversation identifier")
    })

/**
 * @description Conversation resource updated
 */
export const apiConversationsIdPatch200Schema = z.lazy(() => conversationJsonldSchema).describe("Aggregate of {@see InboundEvent}s + {@see OutboundMessage}s that\nbelong to the same logical thread.\n\nOnly threading-capable channels populate this entity — Mail (via\nMessage-ID / References), Slack (thread_ts), WhatsApp (chat-id).\nChannels without thread semantics (Zabbix alerts, CVE feeds,\nmonitoring webhooks) leave their InboundEvents standalone with\n`conversationId = NULL`.\n\n`threadKey` is the channel-specific identifier the\n`ConversationThreader` strategy uses to match incoming events to\nan existing conversation. For mail it's the root References\nvalue; for slack it's the thread_ts of the root message.\n\n`customer`/`assignee` are resolved at first ingest time and may be\noverridden manually in the SPA later.")

/**
 * @description Invalid input
 */
export const apiConversationsIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiConversationsIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiConversationsIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated Conversation resource
 */
export const apiConversationsIdPatchMutationRequestSchema = z.lazy(() => conversationJsonMergePatchSchema).describe("Aggregate of {@see InboundEvent}s + {@see OutboundMessage}s that\nbelong to the same logical thread.\n\nOnly threading-capable channels populate this entity — Mail (via\nMessage-ID / References), Slack (thread_ts), WhatsApp (chat-id).\nChannels without thread semantics (Zabbix alerts, CVE feeds,\nmonitoring webhooks) leave their InboundEvents standalone with\n`conversationId = NULL`.\n\n`threadKey` is the channel-specific identifier the\n`ConversationThreader` strategy uses to match incoming events to\nan existing conversation. For mail it's the root References\nvalue; for slack it's the thread_ts of the root message.\n\n`customer`/`assignee` are resolved at first ingest time and may be\noverridden manually in the SPA later.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'deletedAt': true,'version': true,'deleted': true })

export const apiConversationsIdPatchMutationResponseSchema = z.lazy(() => apiConversationsIdPatch200Schema)