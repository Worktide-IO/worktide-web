/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { workflowJsonldSchema } from "./workflow/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiWorkflowsIdGetPathParamsSchema = z.object({
    "id": z.string().describe("Workflow identifier")
    })

/**
 * @description Workflow resource
 */
export const apiWorkflowsIdGet200Schema = z.lazy(() => workflowJsonldSchema).describe("Workflow = named process template, linked to N projects. Holds N\nAutomations (when-then rules) that fire on domain events emitted by\nany project bound to this workflow.\n\nLinked from Project.workflow (nullable). awork also allows linking from\nProjectTemplate so instantiated projects inherit it — that comes later.")

/**
 * @description Forbidden
 */
export const apiWorkflowsIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiWorkflowsIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiWorkflowsIdGetQueryResponseSchema = z.lazy(() => apiWorkflowsIdGet200Schema)