/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { userCapacityJsonldSchema } from "./userCapacity/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiUserCapacitiesGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"user": z.optional(z.string()),
"user[]": z.optional(z.array(z.string()))
    })

/**
 * @description UserCapacity collection
 */
export const apiUserCapacitiesGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(userCapacityJsonldSchema.describe("Per-user weekly working capacity in MINUTES per weekday. Replaces awork's\n`UserWeeklyCapacity` object — flattened into the row so reporting queries\ndon't need to join an embedded.\n\nOne row per user, enforced by the unique constraint on user_id. Default\n= 0 minutes everywhere; UI should pre-fill 480 (8h) on weekdays for new\nusers."))
              }
    })).describe("UserCapacity.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiUserCapacitiesGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiUserCapacitiesGetCollectionQueryResponseSchema = z.lazy(() => apiUserCapacitiesGetCollection200Schema)