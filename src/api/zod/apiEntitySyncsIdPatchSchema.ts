/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { entitySyncJsonMergePatchSchema } from "./entitySync/jsonMergePatchSchema.ts";
import { entitySyncJsonldSchema } from "./entitySync/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiEntitySyncsIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("EntitySync identifier")
    })

/**
 * @description EntitySync resource updated
 */
export const apiEntitySyncsIdPatch200Schema = z.lazy(() => entitySyncJsonldSchema).describe("One mapping row between a Worktide entity and its mirror in an\nexternal system. Replaces and generalises the per-entity\n{@see Trait\\ExternalReferenceTrait} for cases that need:\n\n  - M:N. A single Worktide Task can be mirrored in Jira AND\n    Redmine AND a CalDAV calendar simultaneously — three\n    EntitySync rows, one per channel.\n  - Bidirectional sync state. The trait only carried a flat ID;\n    this entity carries `externalUpdatedAt` + `ourVersion` + `etag`\n    so adapters can detect dual-changes and apply\n    {@see ConflictPolicy}.\n  - Pause without un-mapping. `syncMode = Disabled` keeps the\n    mapping intact while halting reads/writes — useful during\n    debugging or partial migrations.\n\nThe (entityType, entityId) pair carries the **identity** of the\nWorktide-side record; entityType is a free-form string slug\n(`task`, `project`, `customer`, `time_entry`, `calendar_event`)\nso we don't have to invent a discriminator hierarchy for every\nfuture entity that becomes syncable.\n\nUNIQUE(channel, externalId) makes a single external record map\nto at most one local entity per channel; the inverse is allowed\n(one local entity in N channels), which is exactly the M:N we\nwant.")

/**
 * @description Invalid input
 */
export const apiEntitySyncsIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiEntitySyncsIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiEntitySyncsIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated EntitySync resource
 */
export const apiEntitySyncsIdPatchMutationRequestSchema = z.lazy(() => entitySyncJsonMergePatchSchema).describe("One mapping row between a Worktide entity and its mirror in an\nexternal system. Replaces and generalises the per-entity\n{@see Trait\\ExternalReferenceTrait} for cases that need:\n\n  - M:N. A single Worktide Task can be mirrored in Jira AND\n    Redmine AND a CalDAV calendar simultaneously — three\n    EntitySync rows, one per channel.\n  - Bidirectional sync state. The trait only carried a flat ID;\n    this entity carries `externalUpdatedAt` + `ourVersion` + `etag`\n    so adapters can detect dual-changes and apply\n    {@see ConflictPolicy}.\n  - Pause without un-mapping. `syncMode = Disabled` keeps the\n    mapping intact while halting reads/writes — useful during\n    debugging or partial migrations.\n\nThe (entityType, entityId) pair carries the **identity** of the\nWorktide-side record; entityType is a free-form string slug\n(`task`, `project`, `customer`, `time_entry`, `calendar_event`)\nso we don't have to invent a discriminator hierarchy for every\nfuture entity that becomes syncable.\n\nUNIQUE(channel, externalId) makes a single external record map\nto at most one local entity per channel; the inverse is allowed\n(one local entity in N channels), which is exactly the M:N we\nwant.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true })

export const apiEntitySyncsIdPatchMutationResponseSchema = z.lazy(() => apiEntitySyncsIdPatch200Schema)