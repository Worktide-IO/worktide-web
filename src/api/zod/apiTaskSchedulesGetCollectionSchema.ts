/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { hydraCollectionBaseSchemaSchema } from "./hydraCollectionBaseSchemaSchema.ts";
import { taskScheduleJsonldSchema } from "./taskSchedule/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskSchedulesGetCollectionQueryParamsSchema = z.object({
    "page": z.coerce.number().int().default(1).describe("The collection page number"),
"itemsPerPage": z.coerce.number().int().min(0).max(200).default(30).describe("The number of items per page"),
"workspace": z.optional(z.string()),
"workspace[]": z.optional(z.array(z.string())),
"project": z.optional(z.string()),
"project[]": z.optional(z.array(z.string())),
"isEnabled": z.optional(z.boolean()),
"nextRunAt[before]": z.optional(z.string()),
"nextRunAt[strictly_before]": z.optional(z.string()),
"nextRunAt[after]": z.optional(z.string()),
"nextRunAt[strictly_after]": z.optional(z.string()),
"order[nextRunAt]": z.enum(["asc", "desc"]).default("asc"),
"order[name]": z.enum(["asc", "desc"]).default("asc")
    })

/**
 * @description TaskSchedule collection
 */
export const apiTaskSchedulesGetCollection200Schema = z.lazy(() => hydraCollectionBaseSchemaSchema).and(z.object({
    get "member"(){
                return z.array(taskScheduleJsonldSchema.describe("\"Every X, create a Task in project Y\" — recurring task definition.\n\nMVP: stores a cron expression (`0 9 * * 1` = Mondays 9am) plus a Task\ntemplate inline (title/description/priority/estimatedMinutes). When\n`app:tasks:run-schedules` fires and `nextRunAt <= now`, the schedule\nspawns a Task in `project` with these defaults and re-computes\n`nextRunAt`.\n\nStoring a TaskTemplate FK would be cleaner (reusable templates) but\n\"every X create THIS specific task\" is the 90% use case — inline keeps\nthe data model flat and the cron worker self-contained."))
              }
    })).describe("TaskSchedule.jsonld collection.")

/**
 * @description Forbidden
 */
export const apiTaskSchedulesGetCollection403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiTaskSchedulesGetCollectionQueryResponseSchema = z.lazy(() => apiTaskSchedulesGetCollection200Schema)