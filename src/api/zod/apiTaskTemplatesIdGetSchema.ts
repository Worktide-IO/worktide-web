/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskTemplateJsonldSchema } from "./taskTemplate/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskTemplatesIdGetPathParamsSchema = z.object({
    "id": z.string().describe("TaskTemplate identifier")
    })

/**
 * @description TaskTemplate resource
 */
export const apiTaskTemplatesIdGet200Schema = z.lazy(() => taskTemplateJsonldSchema).describe("Template for a Task — lives inside a TaskBundle. When the bundle is\napplied to a project (or pulled in via instantiate-from-ProjectTemplate),\neach TaskTemplate becomes a real Task with these defaults filled in and\neverything else (assignee, dueDate, status) reset to project defaults.\n\n`dueDayOffset` is days from the project start; the instantiator computes\n`dueOn = project.startsOn + dueDayOffset` (or `null` if startsOn is null).\n`parent` enables one-level subtask templates inside the same bundle.")

/**
 * @description Forbidden
 */
export const apiTaskTemplatesIdGet403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiTaskTemplatesIdGet404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

export const apiTaskTemplatesIdGetQueryResponseSchema = z.lazy(() => apiTaskTemplatesIdGet200Schema)