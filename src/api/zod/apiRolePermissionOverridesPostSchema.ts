/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { rolePermissionOverrideJsonldSchema } from "./rolePermissionOverride/jsonldSchema.ts";
import { rolePermissionOverrideSchema } from "./rolePermissionOverrideSchema.ts";
import { z } from "zod/v4";

/**
 * @description RolePermissionOverride resource created
 */
export const apiRolePermissionOverridesPost201Schema = z.lazy(() => rolePermissionOverrideJsonldSchema).describe("Workspace-scoped override of the DefaultPermissions matrix.\n\nEach row says: in workspace X, role R has capability C with value\n`isGranted` (which can be true OR false — overrides can both grant AND\nrevoke). The {@see PermissionResolver} consults overrides first and only\nfalls back to {@see \\App\\Security\\DefaultPermissions} when no row matches.\n\nThe unique constraint guarantees one override per (workspace, role,\ncapability) tuple — re-asserting the same triple updates the existing row\nrather than stacking conflicting policies.\n\nOwner is intentionally excluded as a target role here (validation): no\noverride should be able to lock the owner out of their own workspace.")

/**
 * @description Invalid input
 */
export const apiRolePermissionOverridesPost400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiRolePermissionOverridesPost403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiRolePermissionOverridesPost422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The new RolePermissionOverride resource
 */
export const apiRolePermissionOverridesPostMutationRequestSchema = z.lazy(() => rolePermissionOverrideSchema).describe("Workspace-scoped override of the DefaultPermissions matrix.\n\nEach row says: in workspace X, role R has capability C with value\n`isGranted` (which can be true OR false — overrides can both grant AND\nrevoke). The {@see PermissionResolver} consults overrides first and only\nfalls back to {@see \\App\\Security\\DefaultPermissions} when no row matches.\n\nThe unique constraint guarantees one override per (workspace, role,\ncapability) tuple — re-asserting the same triple updates the existing row\nrather than stacking conflicting policies.\n\nOwner is intentionally excluded as a target role here (validation): no\noverride should be able to lock the owner out of their own workspace.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true,'granted': true })

export const apiRolePermissionOverridesPostMutationResponseSchema = z.lazy(() => apiRolePermissionOverridesPost201Schema)