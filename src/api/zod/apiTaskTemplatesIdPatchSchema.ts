/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import { constraintViolationJsonldSchema } from "./constraintViolation/jsonldSchema.ts";
import { errorJsonldSchema } from "./error/jsonldSchema.ts";
import { taskTemplateJsonMergePatchSchema } from "./taskTemplate/jsonMergePatchSchema.ts";
import { taskTemplateJsonldSchema } from "./taskTemplate/jsonldSchema.ts";
import { z } from "zod/v4";

export const apiTaskTemplatesIdPatchPathParamsSchema = z.object({
    "id": z.string().describe("TaskTemplate identifier")
    })

/**
 * @description TaskTemplate resource updated
 */
export const apiTaskTemplatesIdPatch200Schema = z.lazy(() => taskTemplateJsonldSchema).describe("Template for a Task — lives inside a TaskBundle. When the bundle is\napplied to a project (or pulled in via instantiate-from-ProjectTemplate),\neach TaskTemplate becomes a real Task with these defaults filled in and\neverything else (assignee, dueDate, status) reset to project defaults.\n\n`dueDayOffset` is days from the project start; the instantiator computes\n`dueOn = project.startsOn + dueDayOffset` (or `null` if startsOn is null).\n`parent` enables one-level subtask templates inside the same bundle.")

/**
 * @description Invalid input
 */
export const apiTaskTemplatesIdPatch400Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Forbidden
 */
export const apiTaskTemplatesIdPatch403Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description Not found
 */
export const apiTaskTemplatesIdPatch404Schema = z.lazy(() => errorJsonldSchema).describe("A representation of common errors.")

/**
 * @description An error occurred
 */
export const apiTaskTemplatesIdPatch422Schema = z.lazy(() => constraintViolationJsonldSchema).describe("Unprocessable entity")

/**
 * @description The updated TaskTemplate resource
 */
export const apiTaskTemplatesIdPatchMutationRequestSchema = z.lazy(() => taskTemplateJsonMergePatchSchema).describe("Template for a Task — lives inside a TaskBundle. When the bundle is\napplied to a project (or pulled in via instantiate-from-ProjectTemplate),\neach TaskTemplate becomes a real Task with these defaults filled in and\neverything else (assignee, dueDate, status) reset to project defaults.\n\n`dueDayOffset` is days from the project start; the instantiator computes\n`dueOn = project.startsOn + dueDayOffset` (or `null` if startsOn is null).\n`parent` enables one-level subtask templates inside the same bundle.").unwrap().omit({ 'id': true,'createdAt': true,'updatedAt': true,'version': true })

export const apiTaskTemplatesIdPatchMutationResponseSchema = z.lazy(() => apiTaskTemplatesIdPatch200Schema)